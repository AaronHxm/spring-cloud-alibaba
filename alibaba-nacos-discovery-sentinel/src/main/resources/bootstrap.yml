server:
  port: 8004
spring:
  application:
    name: alibaba-sentinel-rate-limiting
  cloud:
    sentinel:
      enabled: true
      transport:
        port: 8719
        dashboard: 127.0.0.1:8080
    nacos:
      config:
        enabled: false

