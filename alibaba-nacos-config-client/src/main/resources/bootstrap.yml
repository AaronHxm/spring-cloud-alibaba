server:
  port: 8003
spring:
  application:
    name: alibaba-nacos-config-client
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        ext‐config[0]:
          data‐id: common.yml
          group: DEFAULT_GROUP
          refresh: true
      discovery:
        server-addr: 127.0.0.1:8848
        namespace:

